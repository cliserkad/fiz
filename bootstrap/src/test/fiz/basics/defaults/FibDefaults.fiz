type FibDefaults {
  main {
    // parse the first argument as an i32
    println(fib(5))
    println(fib())
    println(fib(12))
  }

  i32 fib(i32 n= 10) {
    if n <= 1 {
      return n
    } else {
      return fib(n - 1) + fib(n - 2)
    }
  }

}
