type FibDefaults {
  main {
    // parse the first argument as an int
    println(fib(5))
    println(fib())
    println(fib(12))
  }

  int fib(int n= 10) {
    if n <= 1 {
      return n
    } else {
      return fib(n - 1) + fib(n - 2)
    }
  }

}
