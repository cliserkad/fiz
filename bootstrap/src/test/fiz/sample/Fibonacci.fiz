type Fibonacci {
  main {
    // parse the first argument as an i32
    println(fib(parseInt(args[0])))
  }

  i32 fib(i32 n) {
    if n <= 1 {
      return n
    } else {
      return fib(n - 1) + fib(n - 2)
    }
  }

}
