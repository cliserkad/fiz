package test.fiz.files

type Caller {
    // FIXME fails to pull const from Container
    // this is a result of how constants / "static fields" are resolved now
    main {
        // println(Container.CONST_MSG)
        println("hello world")
    }
}
